PORT=5001
NODE_ENV=development
MONGODB_URI=mongodb://localhost:27017/newshub
JWT_SECRET=newshub_super_secret_jwt_key_2026_interview_ready_safe
JWT_EXPIRES_IN=7d
CLIENT_URL=http://localhost:5173

# External News APIs (Optional - built-in editorial fallback provider activates automatically if keys are absent or rate-limited)
GNEWS_API_KEY=
NEWS_API_KEY=
NEWSDATA_API_KEY=

# Cache TTL in seconds (default 10 minutes)
CACHE_TTL_SECONDS=600
